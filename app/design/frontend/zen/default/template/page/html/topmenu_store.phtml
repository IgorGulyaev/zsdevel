<div class="navbar-nav-container">
    <div class="container">
        <nav id="navbar" class="navbar">
            <ul class="nav navbar-nav">
                <?php
                foreach (Mage::app()->getWebsites() as $website) {
                    foreach ($website->getGroups() as $group) {
                        $stores = $group->getStores();
                        foreach ($stores as $store) {
                            echo '<li><a href="'.$store['code'].'">'.$store->getName().'</a></li>';

                            echo '<ul>';
                            $storecategoryid = Mage::app()->getStore($store->getId())->getRootCategoryId();
                            $category = Mage::getModel('catalog/category')->getCollection()->addFieldToFilter('is_active',array('eq' => 1))->load();
                            foreach($category as $cat)
                            {
                                echo '<li><a href="'.$store['code'].'">'.$store->getName().'</a></li>';
                            }
                            echo '</ul>';
                            /*if (!$store['code'] == 'sandbox') {

                            }*/
                        }
                    }
                }
                ?>
            </ul>
        </nav>
    </div>
</div>